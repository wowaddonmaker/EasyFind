if GetLocale() ~= "frFR" then return end
local _, ns = ...
local L = ns.L
if not L then return end

-- French translations stub. Add entries as they're translated; any key
-- omitted falls back to the enUS source. Blizzard UI labels come from
-- `_G[KEY]` lookups in the source code and don't need entries here.
