local _, ns = ...

-- Hand-recorded places on WoW Forever, keyed by Forever's own map ids.
-- Recorded in game with the dev tools' POI recorder (/efd record) and
-- pasted here; nothing from retail applies, since the ids and layouts
-- differ. Empty until the first recording pass.
ns.STATIC_LOCATIONS_FOREVER = {
}
